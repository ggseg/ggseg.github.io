---
title: "Ecosystem"
include-after-body:
  - text: |
      <script type="module">
        import { getCorePackages, getAtlasPackages, getAtlases } from '../js/api.js';
        import { renderCorePackages, renderAtlasPackages, renderAtlases } from '../js/render.js';

        const coreContainer = document.getElementById('core-packages-grid');
        getCorePackages().then(pkgs => renderCorePackages(pkgs, coreContainer));

        const atlasContainer = document.getElementById('atlases-grid');
        let allAtlases = [];

        getAtlases().then(atlases => {
          allAtlases = atlases;
          renderAtlases(atlases, atlasContainer);
          document.getElementById('atlas-count').textContent = atlases.length;
        });

        const searchInput = document.getElementById('atlas-search');
        searchInput.addEventListener('input', (e) => {
          const query = e.target.value.toLowerCase().trim();
          const filtered = allAtlases.filter(atlas =>
            atlas.title.toLowerCase().includes(query) ||
            atlas.name.toLowerCase().includes(query)
          );
          renderAtlases(filtered, atlasContainer);
          document.getElementById('atlas-count').textContent = filtered.length;
        });

        const pkgContainer = document.getElementById('atlas-packages-grid');
        getAtlasPackages().then(pkgs => renderAtlasPackages(pkgs, pkgContainer));
      </script>
---

## Core Packages {.section-header}

The foundational packages for brain atlas visualization in R.

::: {#core-packages-grid .package-grid}
Loading packages...
:::

## Python {.section-header}

::: {.python-section}

::: {.package-card .python-card}

### ggsegpy {.python-pkg-title}

::: {.package-meta}
<span class="experimental-badge">Experimental</span>
<a href="https://github.com/ggsegverse/ggsegpy" target="_blank" class="pkg-badge">
  <img src="https://img.shields.io/github/v/release/ggsegverse/ggsegpy?label=GitHub&color=3572A5" alt="GitHub release">
</a>
:::

Brain atlas visualization in Python using [plotnine](https://plotnine.org){target="_blank"} and [Plotly](https://plotly.com/python/){target="_blank"}.
Supports 2D and 3D brain plots with Desikan-Killiany, aseg, and TRACULA atlases.

```bash
pip install ggsegpy
```

::: {.links}
[GitHub](https://github.com/ggsegverse/ggsegpy){target="_blank"}
:::

:::

:::

## Brain Atlases {.section-header}

The ggsegverse provides <span id="atlas-count">...</span> brain atlases for visualization, each available as 2D polygons and/or 3D meshes.

::: {.atlas-search-box}
<input type="text" id="atlas-search" placeholder="Search atlases by name..." />
:::

::: {#atlases-grid .atlas-dataset-grid}
Loading atlases...
:::

## Atlas Packages {.section-header}

Brain atlas datasets are distributed as R packages.
Install any atlas from R-universe:

```r
install.packages(
 "ggsegYeo2011",
 repos = c(
   "https://ggsegverse.r-universe.dev",
   "https://cloud.r-project.org"
  )
)
```

::: {#atlas-packages-grid .atlas-grid}
Loading packages...
:::

Browse all packages on [R-universe](https://ggsegverse.r-universe.dev/builds){target="_blank"}.
